# Experiments

This folder contains a simple runner to reproduce results across methods using config files.

- Configs live in `experiments/configs/`
- Run with: `python experiments/run_experiments.py --config <path_to_yaml>`
- Outputs are written to `results/tables/` (and optionally `results/figures/`)
